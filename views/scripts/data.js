var DataModels = {};

(function($){	

	DataModels.Category = Backbone.Model.extend({
	});

	DataModels.Product = Backbone.Model.extend({
	});

	DataModels.Item = Backbone.Model.extend({
	});

	DataModels.Pricing = Backbone.Model.extend({
	});

})(jQuery);